local success, GamesData = pcall(function()
    return loadstring(game:HttpGet("https://raw.githubusercontent.com/RedScripter102/Remastered-Script/refs/heads/main/Gamelist"))()
end)

if not success then
    warn("Failed to load script")
    return
end

if typeof(GamesData) ~= "table" then
    warn("Game list is not a table")
    return
end

local FoundGameSupported = false

for PlaceID, Execute in pairs(GamesData) do
    if tonumber(PlaceID) == game.PlaceId then
        FoundGameSupported = true
        pcall(function()
            loadstring(game:HttpGet(Execute))()
        end)
        break
    end
end

if not FoundGameSupported then
    pcall(function()
        game:GetService("StarterGui"):SetCore("SendNotification",{
            Title = "Exploit System",
            Text = "[?] Game Not Supported.",
            Icon = "rbxassetid://7734053426"
        })
    end)
end
